packages:
  - 'packages/*'
  - 'apps/*'

# apps/web (the beta upload -> confirm -> report flow) was started ahead of the
# original "build LAST" plan, deliberately, to get real bills instead of chasing
# a single contact — see packages/tariff-library/PENDING.md for what's still
# unverified in the money path this app calls into.
#
# Reserved for later phases — deliberately NOT created yet:
#   packages/extraction   vision -> schema; the ONLY package allowed to call an LLM
#   packages/greenbutton  Green Button CSV + ESPI XML parsers
onlyBuiltDependencies:
  - esbuild
